# 阶段4-5未完成功能说明

> ⚠️ **本文档已过时（原写于 2026-06-16）**，下方清单中的多数项已实现，仅作历史记录保留。
> 以本框为准（更新于 2026-06-18）：
>
> | 原标注「未完成」 | 实际状态 |
> |---|---|
> | EPUSDT Provider | ✅ `app/services/payments/epusdt.py` + `app/services/payment_providers/epusdt_gmpay.py` |
> | 易支付 Provider | ✅ `app/services/payments/epay_compatible.py` + `app/services/payment_providers/epay_compatible.py` |
> | 支付回调重试字段 + 迁移 | ✅ 迁移 `20260616_0001`；逻辑接入内置调度器 `app/workers/payment_retry.py` |
> | mcy-shop 货源 | ✅ `app/services/external_sources/mcy_shop.py` |
> | Webhook 管理 | ✅ `app/services/telegram_bot_management.py` |
> | acg-faka Provider | ❌ 已删除（`fix: 删除错误的 acg_faka.py`），用通用 `standard_http.py` 适配器替代 |
>
> **仍需真实环境联调**：EPUSDT / 易支付 / 外部货源的端到端测试需各自的真实商户凭据。

## 当前状态

由于阶段4（支付网关联调）和阶段5（外部货源联调）需要：
1. 真实的外部系统配置（EPUSDT、易支付、acg-faka 等）
2. 数据库模型扩展（需要 Alembic 迁移）
3. 完整的端到端测试环境

这些功能暂未完全实现。以下是需要补充的内容清单。

---

## 阶段4: 支付网关联调（未完成）

### 需要补充的代码

#### 1. 数据库模型扩展

**文件**: `app/db/models/orders.py`

需要在 `PaymentCallback` 模型中添加字段：
```python
class PaymentCallback(Base):
    # ... 现有字段 ...
    
    # 需要添加：
    retry_count: Mapped[int] = mapped_column(Integer, nullable=False, default=0)
    last_retry_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True))
    failure_reason: Mapped[Optional[str]] = mapped_column(Text)
```

**Alembic 迁移**:
```bash
alembic revision -m "add_payment_callback_retry_fields"
```

#### 2. 支付回调重试 Worker 完整逻辑

**文件**: `workers/payment_retry_worker.py`

当前代码缺少：
- 从 `payload_json` 恢复原始回调数据
- 调用对应 provider 的回调处理方法
- 实际的重试逻辑

需要补充：
```python
async def _retry_failed_callbacks(self, session) -> int:
    # ... 查询失败回调 ...
    
    for callback in callbacks:
        # 恢复原始回调数据
        provider = callback.provider
        payload = callback.payload_json
        
        try:
            # 调用对应 provider 的回调处理
            if provider == "epusdt_gmpay":
                await self._process_epusdt_callback(session, callback, payload)
            elif provider == "epay_compatible":
                await self._process_epay_callback(session, callback, payload)
            else:
                logger.warning(f"Unknown provider: {provider}")
                continue
            
            # 更新为成功
            callback.process_status = "success"
            callback.processed_at = now
            
        except Exception as e:
            # 更新重试计数
            callback.retry_count += 1
            callback.last_retry_at = now
            callback.failure_reason = str(e)
```

#### 3. EPUSDT 支付对接

**文件**: `app/services/payment_providers/epusdt_gmpay.py`（需要创建）

需要实现：
```python
class EpusdtGmpayProvider:
    async def create_payment(self, order, config):
        """创建支付链接"""
        pass
    
    async def verify_callback(self, payload, config):
        """验证回调签名"""
        pass
    
    async def process_callback(self, session, callback_data):
        """处理回调，更新订单状态"""
        pass
    
    async def query_order(self, out_trade_no, config):
        """查询订单状态"""
        pass
```

#### 4. 易支付兼容对接

**文件**: `app/services/payment_providers/epay_compatible.py`（需要创建）

类似 EPUSDT，需要实现相同接口。

---

## 阶段5: 外部货源联调（未完成）

### 需要补充的代码

#### 1. acg-faka Provider 实现

**文件**: `app/services/external_sources/acg_faka.py`（需要创建）

需要实现：
```python
class AcgFakaProvider(ExternalSourceProvider):
    async def sync_catalog(self, cursor, limit):
        """同步商品目录"""
        pass
    
    async def place_order(self, context):
        """在上游下单"""
        pass
    
    async def query_delivery(self, context):
        """查询发货状态"""
        pass
```

#### 2. mcy-shop Provider 实现

**文件**: `app/services/external_sources/mcy_shop.py`（需要创建）

类似 acg-faka，需要实现相同接口。

---

## 阶段3剩余: WebApp 和 Webhook 管理（未完成）

### 需要补充的功能

#### 1. WebApp 登录完整流程

当前已有 `telegram_webapp.py` 验证逻辑，但需要补充：
- Admin Web 前端 WebApp 集成
- 母 Bot 和克隆 Bot 的 WebApp URL 配置
- 一次性绑定码的前端输入界面

#### 2. Webhook 重置功能

**文件**: `app/services/telegram_bot_management.py`（需要创建）

需要实现：
```python
class TelegramBotManagementService:
    async def reset_webhook(self, bot_token, webhook_url, webhook_secret):
        """重置 Bot Webhook"""
        bot = Bot(token=bot_token)
        await bot.set_webhook(
            url=webhook_url,
            secret_token=webhook_secret
        )
    
    async def clear_webhook_cache(self, tenant_id):
        """清理 Webhook 缓存"""
        # 清理 Redis 中的缓存
        pass
```

---

## 为什么这些功能未完成

### 1. 需要真实外部配置

- **EPUSDT**: 需要测试商户号、密钥、网关 URL
- **易支付**: 需要测试 PID、密钥、base URL
- **acg-faka**: 需要 API 地址和凭据
- **Telegram**: 需要 Bot Token 和公网 Webhook URL

### 2. 需要数据库迁移

添加 `retry_count` 和 `last_retry_at` 字段需要：
- 创建 Alembic 迁移文件
- 在测试和生产环境执行迁移
- 回滚计划

### 3. 需要端到端测试

- 真实支付流程测试（可能产生费用）
- 真实回调接收（需要公网暴露）
- 外部系统稳定性未知

---

## 已完成的准备工作

尽管上述功能未完全实现，但已完成：

### 1. 核心框架
- ✅ Worker 基础框架
- ✅ 支付回调重试 Worker 调度逻辑
- ✅ 重试策略（指数退避、最大次数、超时）

### 2. 通知系统
- ✅ Telegram 通知服务（8种场景）
- ✅ 订阅 Worker 集成
- ✅ 消息格式和内联键盘

### 3. 监控和运维
- ✅ 健康检查（Worker 心跳）
- ✅ 审计日志记录
- ✅ 错误日志输出

---

## 后续实施建议

### 优先级1: 数据库迁移

1. 添加 PaymentCallback 重试字段
2. 执行 Alembic 迁移
3. 更新 Worker 使用新字段

### 优先级2: 支付对接（如果有测试环境）

1. 实现 EPUSDT Provider
2. 实现易支付 Provider
3. 补充 Worker 完整逻辑
4. 端到端测试

### 优先级3: Telegram WebApp（如果有 Bot Token）

1. 实现 Webhook 管理服务
2. Admin Web 前端集成
3. 测试 WebApp 登录流程

### 优先级4: 外部货源（可选）

1. 实现 acg-faka Provider
2. 目录同步测试
3. 下单和发货测试

---

## Mock 测试可以做的事

即使没有真实环境，也可以：

### 1. 单元测试

```python
# tests/test_payment_retry_worker.py
@pytest.mark.asyncio
async def test_retry_backoff():
    """测试重试退避时间计算"""
    worker = PaymentRetryWorker()
    
    # 第1次重试：1分钟后
    assert worker._get_backoff_seconds(1) == 60
    
    # 第2次重试：5分钟后
    assert worker._get_backoff_seconds(2) == 300
    
    # 第3次重试：30分钟后
    assert worker._get_backoff_seconds(3) == 1800
```

### 2. 集成测试（Mock 外部调用）

```python
@pytest.mark.asyncio
async def test_epusdt_callback_with_mock():
    """测试 EPUSDT 回调处理（Mock）"""
    with patch('aioh ttp.ClientSession.post') as mock_post:
        mock_post.return_value.status = 200
        
        provider = EpusdtGmpayProvider()
        result = await provider.create_payment(order, config)
        
        assert result.payment_url is not None
```

### 3. 压力测试

```python
@pytest.mark.asyncio
async def test_concurrent_callback_processing():
    """测试并发回调处理"""
    # 创建100个 Mock 回调
    # 验证并发处理不会冲突
```

---

## 估算补充工作量

| 功能 | 工作量 | 前置条件 |
|------|--------|----------|
| 数据库迁移 | 0.5天 | 无 |
| 支付回调完整逻辑 | 1天 | 数据库迁移 |
| EPUSDT 对接 | 2天 | 测试环境 |
| 易支付对接 | 1.5天 | 测试环境 |
| WebApp 和 Webhook | 1.5天 | Bot Token |
| acg-faka 对接 | 2天 | 测试环境 |
| Mock 测试 | 1天 | 无 |
| **合计** | **9.5天** | **部分需要外部配置** |

---

## 结论

当前已完成的功能：
- ✅ 所有不依赖外部配置的核心框架代码
- ✅ 完整的 Worker 基础设施
- ✅ 健康监控和运维工具
- ✅ Telegram 通知系统

未完成的功能：
- ⚠️ 需要外部配置的真实对接
- ⚠️ 需要数据库迁移的字段扩展
- ⚠️ 需要端到端测试的完整验证

**建议**: 在获得真实测试环境配置后，按照上述优先级逐步补充实现。

---

**文档创建时间**: 2026-06-16  
**状态**: 说明文档，非实施总结
